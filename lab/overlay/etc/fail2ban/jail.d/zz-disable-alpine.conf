# Il laboratorio abilita esplicitamente una sola jail per esercizio. Le due jail
# preattivate dal pacchetto Alpine consumerebbero CPU emulata e confonderebbero il
# testimone (un ban deve appartenere a cyber-auth).
[sshd]
enabled = false

[sshd-ddos]
enabled = false
